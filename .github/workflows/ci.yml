name: CI

on:
  pull_request:

permissions:
  contents: write
  pull-requests: write

jobs:
  test:
    uses: gjtorikian/actions/.github/workflows/ruby_test_suite.yml@main
    secrets:
      gh_token: ${{ secrets.GITHUB_TOKEN }}
